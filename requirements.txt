fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.30.0,<1.0.0
python-dotenv>=1.0.0,<2.0.0
supabase>=2.0.0,<3.0.0
httpx>=0.27.0,<1.0.0
pydantic-settings>=2.0.0,<3.0.0
pydantic>=2.0.0,<3.0.0
python-multipart>=0.0.9,<1.0.0
redis>=5.0.0,<6.0.0
slowapi>=0.1.9,<1.0.0
requests>=2.31.0,<3.0.0

# Direct Postgres access, used only by scripts/migrate.py - the app itself
# talks to the database through the Supabase REST client.
psycopg[binary]>=3.2,<4.0

# Cifra whatsapp_configs.access_token (db/token_crypto.py). Ya
# llegaba como dependencia transitiva de supabase, pero ahora dependemos de ella
# directamente y eso se declara.
cryptography>=42.0,<47.0

# LLM Providers
google-generativeai>=0.7.0,<1.0.0  # Gemini